services:
  frontend:
    image: sixpath-frontend:latest
    container_name: sixpath-frontend
    ports:
      - "3001:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    restart: unless-stopped
